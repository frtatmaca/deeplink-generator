package handler

/*
import (
	"github.com/gin-gonic/gin"
	log "gitlab.zfu.fintek.local/zfu/deeplink/core/logger"
	. "gitlab.zfu.fintek.local/zfu/deeplink/service"
	"net/http"
)

type PokemonHandler struct {
	logger   log.ILogger
	services PokemonService
}

func NewPokemonHandler(logger log.ILogger, services PokemonService) PokemonHandler {
	return PokemonHandler{logger, services}
}

func (p *PokemonHandler) GetPokemons(ctx *gin.Context) {
	response, _ := p.services.GetPokemons()

	ctx.JSON(http.StatusOK, response)
}*/
